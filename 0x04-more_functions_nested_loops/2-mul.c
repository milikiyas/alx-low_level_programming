#include "main.h"
/**
* mul - multiplies two integer
* @a: takes in an integer
* @b: takes in an integer
* Return: always 0 ((success))
*/
int mul(int a, int b)
{
	int product_;

	product_ = a * b;
}
