#include "main.h"
#include<stdio.h>
/**
 * main - entry point
 * @argv: array of strings
 * @argc: size of the array plus one
 * Return: 0 always
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
