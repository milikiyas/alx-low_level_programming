#include "main.h"
#include <stdio.h>
/**
 * _puts - maint entry
 * Description: prints a string followed by a new line
 * @str:  takes in a string value
 * Return: void
 */
void _puts(char *str)
{
	printf("%s\n", str);
}
