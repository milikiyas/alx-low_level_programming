#include <stdio.h>
/**
 * main - Entry Point
 *
 * Return: Always 0 ((success))
*/

int main(void)

{
	char hello ='a';

	char hi ='z';

	for (hello = a; hello <= hi; ++hello)
		putchar(hello);
	return (0);
}
