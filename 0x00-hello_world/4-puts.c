#include <stdio.h>

	/**
	 * main -Entry point
	 * 
	 * Retur: alwas 0 (success)
	 *
	 *
	 */

   	int main(void)
{
	char *s = "\"Programming is like building a multilingual puzzle";
	puts(s);
	return (0);
}
